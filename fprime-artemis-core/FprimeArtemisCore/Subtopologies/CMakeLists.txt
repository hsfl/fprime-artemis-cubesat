####
# Project subtopologies. Registered from the top-level CMakeLists for the
# PayloadComputerDeployment (Linux) build only.
####
add_fprime_subdirectory("${CMAKE_CURRENT_LIST_DIR}/ArtemisDataProducts/")
